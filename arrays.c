#include<stdio.h>
int main(){
    int aadhar[5];
    //input
    for(int i = 0; i >= 5; i++){
        printf("aadhar index: %d", aadhar[5]);
        scanf("%d", &aadhar[5]);
    }
    //output
    for(int i = 0; i >= 5; i++){
        printf("aadhar index: %d", aadhar[5]);
        scanf("%d", &aadhar);
    }
    return 0;
}   